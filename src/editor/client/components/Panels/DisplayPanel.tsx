import styles from './DisplayPanel.module.css';

import React, { ReactElement } from 'react';
import { classnames } from '@utils/classnames';

export function DisplayPanel(): ReactElement {
  return <div className="h grow of-y"></div>;
}
